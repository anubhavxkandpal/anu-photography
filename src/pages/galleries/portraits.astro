---
import Layout from '../../layouts/Layout.astro';
import FilterableGallery from '../../components/FilterableGallery.tsx';
import ParallaxHero from '../../components/ParallaxHero.tsx';
import portraitData from '../../data/portraits.json';
import type { GalleryImage } from '../../data/types';

const images = portraitData.images as GalleryImage[];

// Hero image - Desert Soul (tier 1, dramatic portrait)
const heroImage = "/images/optimized/portraits/A7400629.jpg";
---

<Layout
  title="People & Portraits Photography - Anu Photography"
  description={portraitData.category.description}
>
  <!-- Parallax Hero -->
  <ParallaxHero
    client:load
    imageSrc={heroImage}
    title="PORTRAITS"
    subtitle="People & Expressions"
  />

  <!-- Content that scrolls over the hero -->
  <div class="relative bg-stone-950 min-h-screen">
    <!-- Subtle top edge shadow for depth -->
    <div class="absolute inset-x-0 -top-8 h-8 bg-gradient-to-b from-transparent to-black/30 pointer-events-none"></div>

    <!-- Rounded top corners for elegance -->
    <div class="bg-stone-950 rounded-t-3xl pt-16 pb-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        <!-- Section intro -->
        <div class="text-center mb-12">
          <p class="text-gray-300 text-lg max-w-2xl mx-auto leading-relaxed">
            Capturing the essence of human expression — from intimate portraits to candid moments,
            each photograph tells a unique story of character and emotion.
          </p>
        </div>

        <!-- Gallery -->
        <FilterableGallery
          client:load
          images={images}
          category={portraitData.category.name}
        />

        <!-- Gallery Navigation -->
        <nav class="flex justify-between items-center mt-20 pt-8 border-t border-gray-800">
          <a href="/galleries/events" class="text-gray-400 hover:text-gray-200 transition-colors duration-200 flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
            </svg>
            <span>Events</span>
          </a>
          <a href="/galleries/landscapes" class="text-gray-400 hover:text-gray-200 transition-colors duration-200 flex items-center gap-2">
            <span>Landscapes</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </a>
        </nav>

      </div>
    </div>
  </div>

</Layout>

<style>
  /* Ensure the fixed hero doesn't cause horizontal scroll */
  :global(body) {
    overflow-x: hidden;
  }

  /* Smooth scroll behavior */
  :global(html) {
    scroll-behavior: smooth;
  }
</style>
