# Library Audit — 2026-07-02

## Duplicates

None found.

## Missing files

None found.

## Miscategorized

None found (or ranking pass not run — use `--yes`).

## Promote candidates (top ~15%, currently tier 3)

None found (or ranking pass not run — use `--yes`).

## Demote candidates (bottom ~25%, currently tier 1)

None found (or ranking pass not run — use `--yes`).

## Removal candidates (bottom ~10%)

None found (or ranking pass not run — use `--yes`).
